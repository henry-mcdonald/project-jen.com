import React from 'react'

const Control = ({title,setTitle}) => {
    return (
        <div>
            {/* <input type="textarea" value={title} onChange={e=>setTitle(e.target.value)}/> 
            <button className="metaButton">
                -
            </button>      */}

        </div>
    )
}

export default Control
